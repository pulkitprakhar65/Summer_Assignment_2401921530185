class Solution {
public:
    double findMaxAverage(vector<int>& nums, int k) {
        int n = nums.size();
        double sum = 0;

        for(int i=0 ; i<k ; i++)
        {
            sum = sum + nums[i];
        }

        double maxi = sum;

        for(int j=k ; j<n ; j++)
        {
            sum = sum + nums[j];
            sum = sum - nums[j-k];
            maxi = max(maxi,sum);
        }

        return maxi/k;
    }
};
